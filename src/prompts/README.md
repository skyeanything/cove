# 提示词目录

各功能的系统提示词放在此目录下，便于直接修改文案而无需改代码。

## 约定

- 每个功能一个文件，如 `conversation-title.md` 用于会话自动标题。
- 占位符使用 `{{name}}` 形式，代码中会替换为实际值。当前使用的占位符：
  - **conversation-title.md**：`{{user_message}}` — 用户首条问题内容。

## 使用方式

修改对应 `.md` 文件后保存即可，下次调用时会读取最新内容（开发模式下 Vite 会热更新）。
