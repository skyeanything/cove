import React from "react";
import ReactDOM from "react-dom/client";
import SpikeOfficePreview from "./spike-office-preview";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SpikeOfficePreview />
  </React.StrictMode>,
);
